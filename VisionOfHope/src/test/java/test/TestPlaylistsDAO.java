package test;

import static org.junit.Assert.*;

import java.util.List;

import org.junit.Test;

import cs3733.main.DB.PlaylistsDAO;
import cs3733.main.model.Playlist;

public class TestPlaylistsDAO {

/*	@Test
	public void test() {
		List<Playlist> test = null;
		List<Playlist> correct = null;
		
		Playlist one = new Playlist();
		Playlist two = new Playlist();
		Playlist three = new Playlist();
		correct.add(one);
		correct.add(two);
		correct.add(three);
		
		PlaylistsDAO dao = new PlaylistsDAO();
		try {
			test = dao.listAllPlaylists();
		} catch (Exception e) {
			fail("exception");
		}
		
		assertEquals(correct,test); //im gonna have to use a .equals method but i dont wanna right now
	}
*/
}
